services:
  ${@NAMETAG}.haproxy.requests:
    image: grafana/alloy:v1.8.1
    restart: always
    env_file:
      - ./.${@ALIAS}.${@NAMETAG}.env
    volumes:
      - ./config.${@NAMETAG}.haproxy.requests.alloy:/etc/alloy/config.alloy
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - "run"
      - "--storage.path=/var/lib/alloy/data"
      - "/etc/alloy/config.alloy"

  ${@NAMETAG}.haproxy.metrics:
    image: grafana/alloy:v1.8.1
    restart: always
    pid: host
    env_file:
      - ./.${@ALIAS}.${@NAMETAG}.env
    volumes:
      - ./config.${@NAMETAG}.haproxy.metrics.alloy:/etc/alloy/config.alloy
      - /proc:/proc
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - "run"
      - "--storage.path=/var/lib/alloy/data"
      - "/etc/alloy/config.alloy"
      
  ${@NAMETAG}.docker.logs:
    image: grafana/alloy:v1.8.1
    restart: always
    env_file:
      - ./.${@ALIAS}.${@NAMETAG}.env
    volumes:
      - ./config.${@NAMETAG}.docker.logs.alloy:/etc/alloy/config.alloy
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - "run"
      - "--storage.path=/var/lib/alloy/data"
      - "/etc/alloy/config.alloy"

  ${@NAMETAG}.docker.metrics:
    image: grafana/alloy:v1.8.1
    restart: always
    pid: host
    env_file:
      - ./.${@ALIAS}.${@NAMETAG}.env
    volumes:
      - ./config.${@NAMETAG}.docker.metrics.alloy:/etc/alloy/config.alloy
      - /proc:/proc
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - "run"
      - "--storage.path=/var/lib/alloy/data"
      - "/etc/alloy/config.alloy"

  ${@NAMETAG}.system.logs:
    image: grafana/alloy:v1.8.1
    restart: always
    env_file:
      - ./.${@ALIAS}.${@NAMETAG}.env
    volumes:
      - ./config.${@NAMETAG}.system.logs.alloy:/etc/alloy/config.alloy
      - /var/log:/host/var/log:ro
    command:
      - "run"
      - "--storage.path=/var/lib/alloy/data"
      - "/etc/alloy/config.alloy"

  ${@NAMETAG}.system.metrics:
    image: grafana/alloy:v1.8.1
    pid: host
    restart: always
    env_file:
      - ./.${@ALIAS}.${@NAMETAG}.env
    volumes:
      - ./config.${@NAMETAG}.system.metrics.alloy:/etc/alloy/config.alloy
      - /proc:/proc
      - /sys:/sys:ro
    command:
      - "run"
      - "--storage.path=/var/lib/alloy/data"
      - "/etc/alloy/config.alloy"