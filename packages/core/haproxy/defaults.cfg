log global
log-format "[TS] time=%t client=%ci:%cp backend=%b status=%ST method=%HM uri=%{+E}[capture.req.uri]"
# wach ${@NAMETAG}.docker dns and resolve container ip, automaticly brings UP and DOWN frontends
default-server check resolvers ${@NAMETAG}.docker init-addr last,libc,1.1.1.1
